<% tickets.forEach(function(ticket) { %>
<li class="issue">
  <div class="date"><%= ticket.date %></div>
  <div class="item">
    <span
      id="priority<%= ticket.priority_priority_id %>"
      class="material-symbols-outlined priority"
    >
    </span>
    <button class="status-btn">
      <span class="material-symbols-outlined status unchecked">
        radio_button_unchecked
      </span>
    </button>
    <div class="description">
      <h3><%= ticket.title %></h3>
      <p><%= ticket.description %></p>
    </div>
  </div>
</li>
<% }) %>
